steps:
  - name: Install tmux
    run: |
      # Remove stale Yarn apt source that has an expired GPG key,
      # which prevents apt update from succeeding.
      sudo rm -f /etc/apt/sources.list.d/yarn.list
      sudo apt-get update
      sudo apt-get install -y tmux
